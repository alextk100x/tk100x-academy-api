{
  "name": "tk100x-academy-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute tk100x-academy-db --local --file=./migrations/0001_initial/up.sql",
    "db:migrate:remote": "wrangler d1 execute tk100x-academy-db --remote --file=./migrations/0001_initial/up.sql"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250210.0",
    "wrangler": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
